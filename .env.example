APP_ENV=dev
APP_DEBUG=1
APP_SECRET=change-me-in-dev

DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

DAEMON8_ENABLED=true
DAEMON8_URL=http://127.0.0.1:9077/ingest
DAEMON8_APP=daemon8-demo-symfony

MAILER_DSN=null://null

# Chaos & Fixer scenario — set a provider key to enable.
LLM_PROVIDER_KEY=
